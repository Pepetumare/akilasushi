// Bootstrap completo (bundle) — incluye Collapse, Dropdown, etc.
import 'bootstrap';
import 'bootstrap/dist/css/bootstrap.min.css';

// AOS (animaciones al hacer scroll)
import AOS from 'aos';
import 'aos/dist/aos.css';

AOS.init();
